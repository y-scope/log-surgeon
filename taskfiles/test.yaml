version: "3"

includes:
  build: "build.yaml"

tasks:
  debug:
    deps:
      - "build:debug"
    cmd: "{{.G_LOG_SURGEON_BUILD_DIR}}/debug/tests/unit-test"

  release:
    deps:
      - "build:release"
    cmd: "{{.G_LOG_SURGEON_BUILD_DIR}}/release/tests/unit-test"

  examples-debug:
    deps:
      - "build:examples-debug"
    cmd: "{{.G_EXAMPLES_BUILD_DIR}}/debug/intersect-test"

  examples-release:
    deps:
      - "build:examples-release"
    cmd: "{{.G_EXAMPLES_BUILD_DIR}}/release/intersect-test"
